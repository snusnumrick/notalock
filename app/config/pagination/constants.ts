/**
 * Pagination configuration constants
 * Centralizes pagination-related values used throughout the application
 */

/**
 * Default number of items to show per page
 */
export const DEFAULT_PAGE_LIMIT = 12;
